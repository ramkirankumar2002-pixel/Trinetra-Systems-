# Camera manufacturer adapters

No manufacturer-specific camera or ANPR adapter is registered.

Do not assume RTSP credentials, snapshot URLs, or on-camera ANPR.

Add an adapter here only after the camera model and documented API are available.
